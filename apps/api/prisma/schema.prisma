generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Fase 0: apenas a configuração de conexão. Os primeiros models (Company,
// Worker, Admin) entram na Fase 1 — ver docs/04-roadmap-mvp.md.
